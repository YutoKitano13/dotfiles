---
name: review-assist
description: "PRのコードレビューを補助するスキル。PRブランチにチェックアウト済みの状態で、変更内容を要約し、レビュー順序とポイントを提示する。トリガー: 'レビューをしたい', 'レビューします', 'レビュー補助', 'review this PR', 'PRをレビュー'。"
---

# PR Review Assist

ユーザーがコードレビューを行う際の補助スキル。AIがレビューするのではなく、ユーザーのレビュー作業を効率化するためのガイドを提供する。

**前提**: ユーザーは既に `gh pr checkout` 等でPRブランチにチェックアウト済みの状態でこのスキルを呼び出す。

## 入力

- トリガーフレーズ: 「レビューをしたい」「レビューします」「PRをレビュー」など
- PR URLの指定は不要。現在のブランチに紐づくPRを自動的に取得する

## ワークフロー

### Step 1: PR情報の取得

現在チェックアウト中のブランチに紐づくPRの情報を取得する。`gh pr view` / `gh pr diff` は引数なしで現在のブランチのPRを参照する。

```bash
# PR情報の取得（タイトル、説明、状態、ベースブランチ）
gh pr view --json title,body,state,baseRefName,headRefName,files,additions,deletions,changedFiles

# PRのdiffを取得
gh pr diff
```

- **PRが見つからない場合**: 現在のブランチにPRが紐づいていない可能性がある。ユーザーにブランチの確認を案内する。

### Step 2: 変更内容の深い理解

差分だけでなく、実際のコードファイルを読んで理解する。

1. **変更ファイル一覧を取得**: `gh pr view` の結果から変更ファイルのリストを確認
2. **Readツールで実際のコードを読む**: 変更されたファイルの全体像を把握する
3. **変更ファイルが多い場合（10ファイル以上）**: Exploreサブエージェント（Task tool, subagent_type=Explore）を活用して並列に調査する
4. **関連ファイルの確認**: 変更されたファイルがインポートしているファイルや、変更の影響を受ける可能性のあるファイルも必要に応じて確認する

### Step 3: レビューガイドの出力

以下の形式でユーザーにレビューガイドを提示する。

---

#### 1. PR概要

- **タイトル**: PRのタイトル
- **目的**: PRの説明から抽出した変更の目的（1〜2文）
- **変更規模**: 追加行数 / 削除行数 / 変更ファイル数
- **変更の種類**: 新機能 / バグ修正 / リファクタリング / 設定変更 など

#### 2. 変更の要約

PRで何が行われているかを3〜5行で簡潔に解説する。技術的な背景も含める。

#### 3. 推奨レビュー順序

ファイルを論理的な順序でグループ化し、番号付きリストで提示する。

**順序の決定基準:**
- 型定義・インターフェース → コアロジック → 利用側コード → テスト → 設定ファイル
- 依存関係: ファイルAがファイルBをインポートしている場合、Bを先にレビュー
- 重要度: ビジネスロジックの変更 > ユーティリティ変更 > スタイル変更

**各ファイルの情報:**
- ファイルパス（+追加行数 / -削除行数）
- 変更内容の概要（何がどう変わったか）
- レビューポイント（注目すべき箇所、確認すべき観点）

#### 4. 全体的な注目ポイント（任意）

PR全体を通して特に注意すべき点があれば記載する:
- 破壊的変更の可能性
- エッジケースの考慮
- パフォーマンスへの影響
- セキュリティ上の懸念
- テストカバレッジの十分性

---

## 注意事項

- **AIがレビュー判断をしない**: このスキルはレビューの補助であり、「このコードは問題がある」といった判断は行わない。あくまで変更内容の理解を助け、レビューの効率化を支援する
- **ブランチのチェックアウトはしない**: ユーザーが既にPRブランチにチェックアウト済みであることを前提とする。このスキルがブランチの切り替えを行うことはない
- **diffは必ず `gh pr diff` で取得する**: `git diff` をベースブランチ（main, developなど）に対して実行してはならない。ベースブランチの推測を誤ると、PR本来の差分以上のdiffが表示され混乱を招くため、常に `gh pr diff` を使用すること
- **日本語で出力**: レビューガイドはすべて日本語で出力する
